# Reproducibility over convenience. A build that resolves a different tree than the lockfile
# records is not a build anyone can verify, and this repository's whole claim is verifiability.
save-exact=true
engine-strict=true
fund=false
audit=false
