node_modules/
build/
generated/
tests/.bin/
tests/.latest.json
# The lockfile is not committed: the Graph CLI is a development tool for an optional integration,
# pinned by exact version in package.json and installed at use; its transitive tree is not part of
# what ships, and committing it raised 45 dependency advisories on the repository page that
# describe that tooling, not this code (measured 2026-09-04).
package-lock.json
networks.local.json
