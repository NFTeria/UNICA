{
  "name": "unica-settlement-subgraph",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Indexes the UNICA v5 deployment: the market hook's settlement receipts, the direct settler's receipts, and the product catalogue's sales. Every address is configured per network in networks.json, never hard-coded in a mapping.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network sepolia --network-file networks.json",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "matchstick-as": "0.6.0"
  }
}
