# Copy to .env (ignored). Names only; never commit a value.

# The keystore account name and its PUBLIC address. No private key anywhere, ever.
DEPLOYER_ACCOUNT=default
DEPLOYER=0x0000000000000000000000000000000000000000

# Optional. A keyless public endpoint is the default; set a provider URL here for reliability.
SEPOLIA_RPC_URL=

# Optional. Source verification on Etherscan; Sourcify needs no key.
ETHERSCAN_API_KEY=
