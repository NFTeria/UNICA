/* PLACEHOLDER, OWNED BY ANOTHER SCREEN. The shell references this module unconditionally so the
   reference is already in place when the disclosure ("fold") work lands; replace it wholesale.

   It does nothing on purpose: a page that loads it behaves exactly as a page that does not, so
   the reference can ship ahead of the behaviour without changing a single screen. */
export {};
