{
  "name": "unica",
  "version": "0.1.0",
  "private": true,
  "description": "UNICA — a Uniswap v4 settlement hook, and the merchant payment application built on it.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NFTeria/UNICA.git"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && node apps/web/tests/build.test.mjs && node apps/web/tests/parity.test.mjs && node --test apps/web/tests/*.test.mjs tools/unica-evidence/test/*.test.mjs tools/unica-pos-cli/test/*.test.mjs integrations/graph/unica-v4/schema.test.mjs",
    "format": "prettier --write packages scripts package.json tsconfig.base.json",
    "format:check": "prettier --check packages scripts package.json tsconfig.base.json",
    "check": "npm run format:check && npm run typecheck && npm run build:web && npm run test && npm run verify:candidate",
    "build:web": "node apps/web/build.mjs",
    "verify:candidate": "node scripts/verify-candidate.mjs"
  },
  "devDependencies": {
    "@types/node": "22.7.5",
    "prettier": "3.3.3",
    "typescript": "5.6.3"
  },
  "dependencies": {
    "qrcode-generator": "2.0.4"
  }
}
